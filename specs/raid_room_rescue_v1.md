# Raid Room / Rescue V1 — 実装基準
記録日: 2026-09-08 JST
実装基準SHA: b08e396e657615afd6dfddc05bbec37d21561a25
このチャットのProduct Owner決定を優先する。本書は確定事項と暫定値・未確認事項を区別する。既存監査は完了であり再監査を開始しない。本番配信SHAの確認済みを意味しない。

## 確定事項
- 初級・中級・上級・超級の4難度。「通常レイド」は難度を指す用語として使用しない。
- 総合力参加下限: 初級は総合力制限なし、中級160000、上級200000、超級240000。推奨値と別項目。下限一致は参加可。最終判定はサーバー。
- OwnerはRoomを開始したプレイヤー。参加者待機を必須にせず、Owner・救援参加者とも参加後すぐ戦闘できる。
- Room定員20。Room上限の提示値は初級10、中級10、上級10、超級5。ただし上限の集計範囲は下記未確認として扱う。
- Roomに更新ボタン。報酬情報はダイアログ。
- 救援成功は「救援経由」「難度別必要戦闘数」「必要Contribution Damage」「Raid CLEAR」のAND。
- Activity / Guild Chatの救援からRoomへ遷移。参加者一覧、Raid Result、Present受取の経路を接続する。

## バランス値の扱い
後から変更できる設定として分離し、実機確認で調整する。バランス妥当性研究を実装開始条件にしない。
推奨総合力の目安: 初級は開始直後、中級18〜22万、上級22〜26万、超級26万以上。
想定人数の目安: 初級Owner+1〜2、中級3〜5、上級5〜10、超級10〜20。
救援成功の仮置き候補(戦数/Damage): 初級2/16000、中級2/68000、上級3/205000、超級4/290000。これは確定値ではない。
HP・敵ステータス・報酬品目と数量は承認済み値と混同せず、仮置きの出典・版を記録する。

## 未確認の構造条件
未決定と断定せず、既存資料・Product Owner決定の所在を親が照合する。依存しない実装は継続する。
- Room上限の集計範囲、生成資格・費用・期限・再生成。
- 総合力の判定対象編成・判定時点、既存Lv5解放条件の扱い。
- 救援公開先・権限・頻度、救援経由の帰属・集計開始時点。
- Guild所属を現在／参加時／戦闘Snapshotのどれで表示・判定するか。
- 報酬の品目・資格・Room/日次境界と既存報酬・ランキングの切替。
- 期限後戦闘確定、切替時の開始済み戦闘。
これらをWorker独断で確定仕様として埋めない。権利付与・DB移行・運用切替は対応条件の確認後に実装する。

## Authorityと既存監査
戦闘確定・共有HP・参加資格・貢献・報酬権利はサーバー、UIは表示と操作要求。Presentは既存受取Authorityへ接続する。
既存Replayと戦闘エンジンを再利用する。毒がplayerRawDamageに含まれない現状は今回の初回作業で変更しない。過去再計算・遡及付与は別範囲。
現行HP根拠の不足、毒集計差、本番配信SHA等の監査未確認点は残す。

## 初回の共通実装契約
difficulty IDは beginner / intermediate / advanced / expert（内部識別子の実装選択）。
子Aが新規src/domain/raidRoom.tsに型と純粋な総合力判定を定義し、型確定を子B・Cへ送る。
総合力チェックは参加可否全体と分ける。初級の総合力制限なしを、RP・Room状態等を含む参加許可と誤認させない。
未取得・非有限・負数等の不正総合力を中級以上で通さない。推奨値を参加制限に流用しない。
UI用DTOにはサーバー算出の参加資格と理由を持たせる。報酬資格をブラウザ計算で確定しない。
