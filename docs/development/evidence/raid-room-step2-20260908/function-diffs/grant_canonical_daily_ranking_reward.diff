diff --git a/docs/development/evidence/raid-room-step2-20260908/function-diffs/grant_canonical_daily_ranking_reward.preview.sql b/docs/development/evidence/raid-room-step2-20260908/function-diffs/grant_canonical_daily_ranking_reward.candidate.sql
index 32f60f1..69c9c84 100644
--- a/docs/development/evidence/raid-room-step2-20260908/function-diffs/grant_canonical_daily_ranking_reward.preview.sql
+++ b/docs/development/evidence/raid-room-step2-20260908/function-diffs/grant_canonical_daily_ranking_reward.candidate.sql
@@ -1,6 +1,8 @@
-CREATE OR REPLACE FUNCTION public.grant_canonical_daily_ranking_reward(p_ranking_day_key date, p_ranking_type text, p_recipient_user_id uuid, p_ranked_entity_id uuid, p_rank_position integer, p_score bigint)
- RETURNS integer
- LANGUAGE plpgsql
- SECURITY DEFINER
- SET search_path TO 'public'
-AS $function$
+create or replace function public.grant_canonical_daily_ranking_reward(
+  p_ranking_day_key date,
+  p_ranking_type text,
+  p_recipient_user_id uuid,
+  p_ranked_entity_id uuid,
+  p_rank_position integer,
+  p_score bigint
+) returns integer language plpgsql security definer set search_path=public as $$
@@ -8,0 +11 @@ begin
+  if p_ranking_type='RAID_PERSONAL' then return 0; end if;
@@ -53,2 +56 @@ end;
-$function$
-;
\ No newline at end of file
+$$;
\ No newline at end of file
