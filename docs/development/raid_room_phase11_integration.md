# 第11工程 親統合記録

基準SHA: `8947a77714f1f9ee80e955aebfe4d2e348f0c3e0`。2026-09-08。状態VALIDATED（親レビュー・機械検証完了、Human PASSではない）。

## 範囲

Aは未確認開始一覧・確認済み印・未開始要求取消、Bはサーバーからの復帰と共通ダイアログ取消導線、Cは実SQLと実hook、親は差分/検証/PR統合を担当する。

取消は開始と同じusers行lockで直列化する。既存receiptがあれば開始済みとして返し、なければ取消台帳で後続開始を拒否する。確認済み印は復帰一覧から外す表示用メタデータであり、消費・HP・報酬・ランキングを変更しない。

## 親レビュー

SQL255とSQL258の開始関数を本文比較し、CREATE OR REPLACEとusers行lock直後の取消拒否2行以外は一致を確認。既存の開始条件・Snapshot構築・消費・乱数・戦闘計算は維持する。

## 検証

親が対象を確認して再実行した。

|検証|結果|
|---|---|
|PGlite実SQL250〜258、取消/開始順序・一覧/ack・権限|11件PASS|
|共通domain/controller/adapter/Edge route|71件PASS|
|既存Room React操作|20件PASS|
|実useBattle・保存消失/取消再試行/旧経路|17件PASS（既存13件を含む）|
|全体Mock build|PASS、静的生成13ページ|
|最終全体TypeScript|PASS|
|git diff --check|PASS|

SQL258 SHA256 `d852c8acefa220fe4bc72210b2ff362e2b9d8ae12e82c2732e3229c7a32193de`。
最終useBattle SHA256 `5121fd7c330d1b565acf59289f443a2a2cb1551c619cf46ee3f52993143a6968`。

取消失敗時のlocal保持と同ダイアログ再試行、成功時のエラー解除、共通操作ブロックの解放を確認。確定済みの場合は同Replayへ戻り、新しい開始や返金は行わない。

SQLの開始/取消は単一接続の両実行順序で検証した。多接続の待機・競合の検証済みではない。実hookはjsdomと通信doubleであり、実Auth/実端末の保存消失試験ではない。全体buildはMock設定で実行した。詳細は [C検証記録](raid_room_phase11_validation.md)。

## 救援の依存条件

Repositoryの新Room仕様と過去の決定を照合したが、救援の公開先・依頼資格・頻度・帰属時点・新報酬資格とランキング切替の確定本文は確認できなかった。Activityへ救援依頼と撃破を表示する提案は見つかったものの、承認済みとは断定しない。既存の救援成功AND条件と参加下限は維持する。

## 運用

運用フラグfalse。実DBへの適用・Deployは実施しない。今回の一覧/ack/取消を使うUIにはSQL258の適用確認が必要。実DB・多接続・実機を機械検証と区別する。

## 導入の限界

既存Room開始台帳へACK列がNULLで追加されるため、以前のUIで既に閲覧済みだったかは判別しない。推測のBackfillは行わない。未運用のRoom機能へ導入する前提をRelease時に照合する。

有効なlocal記録なしの通常起動は製品Room UIが有効な場合のみserver一覧を読む。無効時は旧起動を維持する。破損localから未知の要求IDを安全に取り消す処理は未実装だが、server保存済みの記録は復帰できる。複数タブの互換セッションinsert一意性は既存の残件。

救援/報酬/ランキング接続と実DB/実Cron/実機確認は未完了。全体開発完了・実機確認可能な一連のPreview到達ではない。
