# 第14工程 親統合記録

2026-09-08。基準SHA `bb6782c5303fedcea1570f603538811b236f03b6`。VALIDATED。

## 範囲

Aは新旧ランキング・主催者/通常参加者報酬の根拠を限定照合。Bは救援依頼UUIDを本人/Roomごとに保存し、通信不明後の再読込でも同要求で再送。Cは保存/再mount/隔離と既存画面の回帰検証。親は契約・根拠・実装レビュー・全体検証・PR反映。

「通信再送は新たな依頼に数えない」という既存条件への対応。サーバーで送信済みの同requestを先に返す経路を再利用し、DB変更はしない。新しい依頼の公開先/回数/権限は変更しない。

## 検証

凍結後に親が共通83件（永続化新4件含む）、React28件（新4件含む）、Mock build、最終TypeScript、git diff --checkのPASSを確認。SQL/戦闘確定は変更しないため既存SQL/useBattleの追加再実行は行っていない。

親レビューで参照中のunmountが他操作のblockを解除し得る点を指摘し、自分の依頼処理のblock所有refだけを解除するよう修正。最終React検証は修正後。Aの根拠文書は新Room仕様と8月22日Freeze等の引用元を親が照合済み。Aは文書レビュー完了でありランキングコード実装ではない。

再読込への対応と、複数タブでの同時新規依頼の完全排他は区別する。実DB/実機確認は未実施。

## 残件

新Roomのランキング切替・主催者/通常参加者別報酬は未提示条件を独断FIXしない。成功閾値/品目数量投入、実DB/実Cron/実機の受入と運用有効化は別工程。第13工程VALIDATEDを全体完成と扱わない。
